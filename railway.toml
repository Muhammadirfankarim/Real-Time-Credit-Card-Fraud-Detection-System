[build]
builder = "nixpacks"

[deploy]
startCommand = "cd api && uvicorn main_mlflow:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
